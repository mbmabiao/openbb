# Boundary Tester 更新计划

## 目标

把当前 `boundary_tester` 从“主要验证 breakout 事件质量”的框架，升级为“同时验证 zone 本身是否有效 + 验证 breakout 质量”的研究框架。

---

## 问题 1：当前测试的核心对象偏了，测到的是 breakout 质量，不是 zone 有效性

### 1）问题

当前主链路是：

* `detect_boundary_events()` 先识别 test / probe / breakout / failed_breakout / retest
* `label_breakout_events()` 只对 `BREAKOUT_EVENT_TYPES` 打标签
* `build_summary_table()` 最终也只统计 breakout 事件

这导致当前 summary 本质上回答的是：

* 这个 zone 触发 breakout 后，后面有没有走出来？

而不是：

* 这个 zone 本身有没有起到边界作用？
* 它更擅长防守，还是更擅长放行突破？

也就是说，当前框架会系统性忽略一类很重要的“有效 zone”：

* 价格碰到 zone 后被压回 / 被托住，没有发生 breakout，但 zone 明明是有效的

### 2）要怎么改

#### 改造目标

把验证拆成两条主线：

1. `zone defense validation`：验证 zone 是否挡得住价格
2. `zone breakout validation`：验证 zone 被突破后，突破是否高质量

#### 具体改法

##### A. 新增防守标签模块

新增文件：

* `src/boundary_tester/defense_labeler.py`

新增函数：

* `label_zone_defense_events(events_df, price_df, zone_df, config)`

只针对 `test` 和 `probe` 事件打标签，输出例如：

* `defense_label`: `hold` / `failed_hold` / `unresolved`
* `reversal_strength_pct`
* `max_rejection_excursion`
* `max_penetration_excursion`
* `bars_to_reversal`
* `close_back_from_edge_flag`
* `zone_defense_score`

##### B. pipeline 改成双标签输出

修改文件：

* `src/boundary_tester/pipeline.py`

把当前：

* `labeled_events_df = label_breakout_events(...)`

改成并行输出：

* `breakout_labeled_events_df = label_breakout_events(...)`
* `defense_labeled_events_df = label_zone_defense_events(...)`

最终 `result` 中新增：

* `defense_labeled_events`
* `breakout_labeled_events`
* `zone_defense_summary`
* `zone_breakout_summary`

##### C. reporter 拆双 summary

修改文件：

* `src/boundary_tester/reporter.py`

新增：

* `build_zone_defense_summary_table()`
* `build_zone_breakout_summary_table()`

最终报告中分为两部分：

1. Zone Defense Summary
2. Breakout Continuation Summary

---

## 问题 2：`is_first_test` / `prior_test_count` 口径不对，被 event emission 污染了

### 1）问题

当前 `_detect_pre_breakout_events()` 里用的是一个统一的 `prior_interaction_count`，只要之前发出过任意 event：

* `test`
* `probe`
* `breakout`

后续 breakout 的 `is_first_test` 就不再是 true。

这会导致：

* `first_test` 被 probe/test 过早吃掉
* “首测”实际上不是按真实 price touch 次数定义的
* `by_first_test_flag` 的研究价值下降

当前这个变量本质上不是：

* zone 被真实测试了几次

而是：

* zone 发射过多少个 event

### 2）要怎么改

#### 改造目标

把“事件去重”与“结构统计”彻底解耦。

#### 具体改法

##### A. 在 event detector 中单独维护真实 touch 计数

修改文件：

* `src/boundary_tester/event_detector.py`

新增概念：

* `touch_count`
* `meaningful_touch_count`
* `bars_since_last_touch`
* `days_since_last_touch`

定义建议：

* `touch`: high/low 与 zone 有真实接触
* `meaningful_touch`: 接触后 close 回到 zone 边界附近，或者 penetration 超过最小阈值

##### B. `_build_event_dict()` 不再接收 `prior_test_count`

改为接收一个更清晰的结构：

* `touch_count_before_event`
* `meaningful_touch_count_before_event`
* `is_first_meaningful_test`

事件字典改为输出：

* `touch_count_before_event`
* `meaningful_touch_count_before_event`
* `is_first_meaningful_test`
* `bars_since_last_touch`

##### C. summary 改口径

修改文件：

* `src/boundary_tester/reporter.py`

把当前：

* `first_test_bucket = is_first_test`

替换为：

* `first_meaningful_test_bucket = is_first_meaningful_test`
* 并新增：`touch_count_bucket`

例如：

* `first_touch`
* `second_touch`
* `third_touch`
* `4plus_touch`

---

## 问题 3：`max_event_gap` 去重逻辑会扭曲 interaction 统计

### 1）问题

当前事件发射用了 `max_event_gap` 限制近距离重复事件，工程上是合理的，但研究上有副作用：

* 你记录到的不是“真实发生了几次触碰”
* 而是“经过压缩后的事件次数”

如果后续直接用这些 event 数量研究首测/复测质量，就会把去重规则误当成市场结构。

### 2）要怎么改

#### 改造目标

保留事件去重，但不要让它污染结构层统计。

#### 具体改法

##### A. detector 输出两层数据

修改文件：

* `src/boundary_tester/event_detector.py`

新增两个层次：

1. `raw_interactions`

   * 记录所有真实接触/穿透
2. `emitted_events`

   * 保留当前 max gap 去重后的事件

##### B. 研究指标优先用 raw interaction 派生

* `first test`
* `repeated test`
* `touch density`
* `time since last touch`

这些指标都从 `raw_interactions` 统计，而不是从 emitted events 统计。

##### C. config 明确拆成两类参数

修改文件：

* `src/boundary_tester/config.py`

当前的：

* `max_event_gap`

建议拆为：

* `event_emission_gap_bars`
* `touch_merge_gap_bars`

前者用于输出 event 去重，后者用于结构分析时合并相近 touch。

---

## 问题 4：zone 样本不独立，同一结构可能被重复计数

### 1）问题

当前 zone 是按 snapshot 周期性重新生成的，`zone_id` 格式是：

* `ticker::YYYYMMDD::side::rank`

这意味着：

* 同一个本质相同的结构，可能在相邻 snapshot 里被生成成多个 zone_id
* 同一段行情可能反复贡献多个 event 样本

研究上会导致：

* 独立样本数被高估
* 某一段趋势或震荡 regime 被重复刷样本

### 2）要怎么改

#### 改造目标

引入“结构级 zone identity（身份）”，区分：

* snapshot zone
* structural zone

#### 具体改法

##### A. 给 zone 新增 `structural_zone_key`

修改文件：

* `src/boundary_tester/zone_engine.py`

在 `build_zone_rows_from_snapshot()` 中新增：

* `structural_zone_key`

建议组成：

* `ticker`
* `side`
* `rounded_center_bucket`
* `rounded_width_bucket`
* `source_types`
* `timeframes`

例如：

* center 按 0.5% 或 1 ATR 做 bucket
* width 按 0.25% 或 ATR 比例做 bucket

##### B. 新增 structural merge

新增函数：

* `merge_snapshot_zones_into_structural_zones(zone_df)`

作用：

* 把相邻 snapshot 中本质相同的 zone 合并到同一个 structural zone 下面

##### C. summary 同时提供两种样本口径

report 中同时输出：

1. event-level summary
2. structural-zone-level summary

这样你就能区分：

* 事件数量多
* 和独立结构真的多

---

## 问题 5：success label 太依赖固定阈值，没有考虑不同波动 regime 和 zone 宽度

### 1）问题

当前 success 主要取决于：

* `success_move_pct = 0.03`
* 或可选 ATR filter

这会有几个问题：

* 不同 ticker 波动水平差异大，固定 3% 不公平
* 不同 zone 宽度差异大，突破窄 zone 和宽 zone 不能同标准
* 突破质量不只取决于“有没有涨够/跌够”，还要看能否站稳、retest 后能否继续

### 2）要怎么改

#### 改造目标

把 success 从“固定百分比 hit target”升级成“多条件 breakout quality 评估”。

#### 具体改法

##### A. config 新增多维 breakout 成功标准

修改文件：

* `src/boundary_tester/config.py`

新增参数：

* `success_move_mode`: `fixed_pct` / `atr` / `zone_width` / `hybrid`
* `success_move_zone_width_multiple`
* `hold_outside_bars_required`
* `require_retest_success`
* `min_breakout_distance_pct`
* `min_breakout_distance_atr`

##### B. labeler 中引入多维成功判定

修改文件：

* `src/boundary_tester/labeler.py`

把当前 success：

* `target_hits.any() and not reentry_flag`

改成分层判定：

1. `breakout_confirmed_flag`

   * 是否真正站到 zone 外
2. `hold_outside_flag`

   * 是否连续 N 根 bar 保持在 zone 外
3. `target_hit_flag`

   * 是否达到 ATR / zone width / fixed pct 目标
4. `retest_success_flag`

   * retest zone 后是否继续沿 breakout 方向运行

最终 success 建议：

* `success = breakout_confirmed_flag and hold_outside_flag and (target_hit_flag or retest_success_flag) and not fast_fail_flag`

##### C. 输出更多质量字段

新增输出列：

* `move_over_atr`
* `move_over_zone_width`
* `hold_outside_bars`
* `retest_success_flag`
* `breakout_quality_score`

---

## 问题 6：false breakout 定义偏短视，只盯 early re-entry

### 1）问题

当前 false breakout 主要看 early window 内是否重新进入 boundary 内，或者 deep reentry。这个对于快速失败有效，但会漏掉：

* 先横一段再失败的 breakout
* retest 后转弱的 breakout
* 表面没快速 reentry，但后续完全没有 continuation 的 breakout

### 2）要怎么改

#### 改造目标

把 false breakout 定义分层。

#### 具体改法

##### A. failure 拆成三类

修改文件：

* `src/boundary_tester/labeler.py`

新增 failure taxonomy：

1. `fast_false_breakout`

   * early reentry / deep reentry
2. `late_failure`

   * 初期没死，但在 lookahead 内回到 zone 内并失效
3. `failed_follow_through`

   * 没明显 reentry，但没有形成任何有效延续

##### B. 输出 failure subtype

新增字段：

* `failure_subtype`
* `bars_to_failure`
* `reentry_depth_pct`
* `post_breakout_best_close_pct`

##### C. reporter 中单独分组

新增：

* `by_failure_subtype`

这样你就能看清楚：

* 失败主要是立刻假突破
* 还是后续没跟进

---

## 问题 7：zone ranking 时已经用了 reaction_score，后续再测 zone，有 selection bias

### 1）问题

当前 `rank_zones_for_side()` 在选 zone 时已经使用：

* `reaction_score`
* `institutional_score`

也就是说，你并不是在“所有候选 zone”上做无偏验证，而是在“历史看起来更有反应的 zone”上再做测试。

这会导致：

* zone 被验证前已经经过先验筛选
* 最终结果混合了 zone engine 的选择偏差

### 2）要怎么改

#### 改造目标

把“候选 zone 生成”和“研究集筛选”分离。

#### 具体改法

##### A. zone_engine 输出全量候选 zone

修改文件：

* `src/boundary_tester/zone_engine.py`
* `src/boundary_tester/auto_runner.py`

当前流程是：

* 候选 zone -> merge -> rank -> 只保留 top N

建议新增两层输出：

1. `candidate_zones`
2. `selected_zones`

##### B. boundary tester 支持测试不同 universe

新增参数：

* `zone_universe_mode`: `selected_only` / `all_candidates` / `top_k_per_side`

##### C. validation 报告明确区分

report 中明确写：

* 这是对 `selected zones` 的研究结果
* 还是对 `all candidate zones` 的研究结果

这样后面你才能分清：

* 是 zone engine 选得准
* 还是某类 zone 结构本身就有效

---

## 问题 8：当前 summary 维度太少，不够支撑研究闭环

### 1）问题

现在 summary 主要按：

* zone_class
* first_test_flag
* timeframe
* confluence_bucket

这些维度虽然有用，但不足以支撑你真正建立可复用验证框架。

### 2）要怎么改

#### 改造目标

让 summary 变成真正可用于 alpha research 的研究输出。

#### 具体改法

##### A. 新增分层字段

建议在 zone/event 记录里新增：

* `touch_count_bucket`
* `zone_width_bucket`
* `distance_to_zone_bucket`
* `institutional_score_bucket`
* `reaction_score_bucket`
* `breakout_distance_over_atr_bucket`
* `hold_outside_bucket`

##### B. reporter 新增分组输出

修改文件：

* `src/boundary_tester/reporter.py`

新增：

* `by_touch_count`
* `by_zone_width_bucket`
* `by_reaction_score_bucket`
* `by_breakout_quality_bucket`
* `by_failure_subtype`

##### C. 新增结构级指标

例如：

* `structural_zone_count`
* `event_per_structural_zone`
* `median_follow_through`
* `median_mfe`
* `median_mae`
* `time_to_failure`
* `hold_rate_after_breakout`

---

## 问题 9：当前输出结构不够适合做后续研究与复用

### 1）问题

当前输出主要是：

* `events.csv`
* `labeled_events.csv`
* `summary.csv`
* `report.md`

对于后续 alpha research 和 validation framework 复用来说，还不够细。

### 2）要怎么改

#### 改造目标

把输出升级成“研究友好”的中间层产物。

#### 具体改法

新增输出文件：

* `raw_interactions.csv`
* `breakout_labeled_events.csv`
* `defense_labeled_events.csv`
* `structural_zones.csv`
* `zone_breakout_summary.csv`
* `zone_defense_summary.csv`
* `research_slices.json`

让后续：

* notebook 分析
* streamlit 展示
* alpha module 筛选

都能直接复用。

---

## 推荐实施顺序

### Phase 1：先修正研究口径

优先级最高：

1. 重写 `is_first_test` / `touch_count`
2. 拆出 `zone defense` 与 `breakout` 双标签
3. 引入 `failure_subtype`

### Phase 2：修正样本独立性与标签公平性

4. 引入 `structural_zone_key`
5. success 改为 ATR / zone width / hold / retest 多条件
6. 区分 `raw_interactions` 与 `emitted_events`

### Phase 3：增强研究框架

7. 支持 `all_candidates` vs `selected_only` 两套 universe
8. 扩展 reporter 分层维度
9. 增加研究友好型输出文件

---

## 最终预期效果

完成以上更新后，这套框架就不再只是：

* “哪些 breakout 看起来更容易成功？”

而会升级为：

1. 哪些 zone 真有边界效应
2. 哪些 zone 更擅长防守
3. 哪些 zone 被突破后更容易形成趋势延续
4. 哪些结构只是表面上有 zone，实则无效
5. 哪些 zone 特征适合作为后续 alpha research 的输入特征

这才更接近你要做的：

* 可复用的边界识别引擎
* 可复用的 breakout event engine
* 可复用的 validation framework
* 可复用的 alpha research module
