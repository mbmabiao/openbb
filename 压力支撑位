# Boundary Tester 当前代码检查：疑似 BUG / 高优先级问题清单

> 说明：
> - 下面不是泛泛建议，而是我认为**会影响结果正确性、研究可信度或系统稳定性**的问题清单。
> - 我按优先级分成：P0 / P1 / P2
> - P0 = 必须先修，不然统计结果不可信
> - P1 = 强烈建议尽快修，不然定义会偏
> - P2 = 工程健壮性与后续扩展问题

---

## P0-01：`event_detector` 与 `labeler` 的 `bar_index` 坐标系不一致

### 位置
- `src/boundary_tester/event_detector.py`
- `src/boundary_tester/labeler.py`

### 现象
`event_detector.py` 中：
- 针对每个 zone，会先对价格数据做局部切片：
  - 按 ticker 过滤
  - 按 `valid_from` 过滤
  - 按 `valid_to` 过滤
  - 然后 `reset_index(drop=True)`
- 随后把这个局部切片里的 `idx` 存入事件字段 `bar_index`

`labeler.py` 中：
- 再次按 ticker 取完整 `price_df`
- 再 `reset_index(drop=True)`
- 直接使用事件中的 `bar_index` 去切 future window

### 本质问题
这两个 `bar_index` **不属于同一坐标系**：

- `event_detector` 里的 `bar_index` = **zone 有效窗口内的局部索引**
- `labeler` 里的 `price_df` = **完整 ticker 时间序列**

因此 `labeler` 里 future window 很可能从错误位置开始切，导致标签错位。

### 影响
会直接污染：
- `label`
- `success_flag`
- `failure_flag`
- `unresolved_flag`
- `max_favorable_excursion`
- `max_adverse_excursion`
- `follow_through_pct`
- `bars_to_reentry`
- `bars_to_target`
- `summary.csv`
- `report.md`

### 风险等级
**P0 / 致命**

### 建议修复方案
#### 推荐方案（最稳）
不要把 `bar_index` 作为跨模块主定位键。

改为：
- 事件中保留 `event_timestamp`
- `labeler` 中使用：
  - `ticker`
  - `event_timestamp`
来回到完整价格序列中重新定位 breakout bar

#### 备选方案
同时存两个索引：
- `local_bar_index`
- `global_bar_index`

然后 labeler 一律使用 `global_bar_index`

## P0-02：yfinance 1h 数据约束检查与官方限制不一致

### 位置
- `src/boundary_tester/auto_runner.py`
- 函数：`validate_provider_constraints`

### 现象
当前代码里对 yfinance 1h 数据的起始时间限制使用了：
- `today - 729 days`

### 本质问题
这与 yfinance 官方文档对 intraday 数据限制不一致。  
官方文档写的是：
- intraday 数据不能超过最近 60 天

因此当前代码属于：
- 使用了一个“经验假设窗口”
- 而不是 provider 文档约束

### 影响
会造成：
1. 系统以为可以合法拉更长 1h 数据
2. 实际 provider 返回空、截断或不稳定
3. zone 生成阶段出现误判“数据足够”
4. 后续验证结果建立在不完整数据上

### 风险等级
**P0 / 高风险**

### 建议修复方案
对 yfinance provider 采用保守硬限制：
- 1h / intraday 统一按官方文档处理
---
## P1-01：`false_breakout_rate` 当前等于 `failure_rate`，统计定义不准确
### 位置
- `src/boundary_tester/reporter.py`
- 函数：`build_summary_table`
### 现象
当前：
- `false_breakout_rate = failure_flag.mean()`

### 本质问题
`failure` 不一定都等于 `false breakout`

举例：
- breakout 后没有快速 reentry
- 但 follow-through 不够强
- 这可能是“失败延续”，未必是“假突破”

### 影响
会导致报告中：
- `false_breakout_rate` 被高估
- 假突破与弱延续被混为一谈
- 误导对 zone 边界质量的判断

### 风险等级
**P1 / 统计定义问题**

### 建议修复方案
拆成两个指标：

#### 1. `false_breakout_rate`
定义为：
- `reentry_flag == 1`
- 或明确识别到 `failed_breakout_up/down`

#### 2. `failed_follow_through_rate`
定义为：
- 未 reentry
- 但没有达到 success threshold

### 推荐动作
- 尽快修
- 同时修改 report 文案和 summary 字段名

---

## P1-02：`failed_breakout` 定义过于宽松，容易把正常回踩误判成失败突破

### 位置
- `src/boundary_tester/event_detector.py`
- 函数：`_detect_post_breakout_events`

### 现象
当前逻辑大致是：

#### resistance side
- breakout 后若 `failure_reentry_bars` 内 `close <= boundary`
- 就可视为 failed breakout

#### support side
- breakout 后若 `close >= boundary`
- 就可视为 failed breakout

### 本质问题
这个定义太敏感：
- 很多正常回踩会暂时 close 到 boundary 附近
- 甚至轻微回到边界里一点点
- 但后续仍可能延续成功

当前代码没有要求：
- reentry 深度
- 连续 reentry bar 数
- 回到 zone center / deeper-in-zone

### 影响
会高估：
- `failed_breakout`
- `failure_rate`
- `false_breakout_rate`

### 风险等级
**P1 / 事件定义偏差**

### 建议修复方案
failed breakout 应至少满足更严格条件之一：
- close 回到 zone 深处（如超过 `upper - x% * zone_width`）
- 连续 N 根 close 回到 zone 内
- close 回到 zone center 另一侧
- reentry 后出现显著 adverse continuation

### 推荐动作
- 参数化 reentry depth / reentry bars

---

## P1-03：breakout 确认在事件层和标签层重复计算，容易导致 success 偏低

### 位置
- `src/boundary_tester/event_detector.py`
- `src/boundary_tester/labeler.py`

### 现象
当前流程中：

#### 事件层
breakout 已要求：
- 连续 `min_close_outside_zone` 根 bar 收在边界外

#### 标签层
又要求：
- future 前 `min_close_outside_zone` 根 bar 也要全部 outside

### 本质问题
这是“双重确认”：
- breakout 事件本身已经做了 outside persistence 检查
- labeler 又重复检查一次
- 且 future_df 不包含 breakout 当根 bar

于是 success 条件比表面参数看起来更严格。

### 影响
- success 样本被压低
- failure / unresolved 被抬高
- zone 好坏判断偏悲观

### 风险等级
**P1 / 标签逻辑偏差**

### 建议修复方案
二选一：

#### 方案 A（推荐）
- 事件层负责 breakout confirmation
- 标签层不再重复检查 persistence

#### 方案 B
- 标签层保留，但把 breakout bar 也纳入 outside persistence 逻辑

### 推荐动作
- 并更新 PRD / 配置注释
